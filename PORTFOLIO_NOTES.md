# Release scope

This frontend showcase contains independently invented data only. It has no
production service configuration, file upload flow, login or AI functionality.
It is intentionally smaller than the private production application.

Keep repository visibility private until the owner chooses to publish.
Deleting or replacing Git history does not recall previously downloaded copies
or guarantee removal of cached hosting-provider objects.
